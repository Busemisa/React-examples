/*
import PropTypes from 'prop-types';
/PropTypes Nedir?
MyComponent.propTypes = {
    / string türünde bir prop
    name: PropTypes.string,
  
    / number türünde bir prop, ve zorunlu (isRequired)
    age: PropTypes.number.isRequired,
  
    / boolean türünde bir prop, ve varsayılan değeri true
    isAdmin: PropTypes.bool,
  
    / array türünde bir prop, ve zorunlu
    hobbies: PropTypes.array.isRequired,
  
    / object türünde bir prop
    address: PropTypes.object,
  
    / fonksiyon türünde bir prop
    onClick: PropTypes.func
  };

  ////////////////////////////////


  /DefaultProps Nedir?
  MyComponent.defaultProps = {
    name: 'John Doe',
    isAdmin: false,
    hobbies: ['Reading', 'Cooking']
  };
  */